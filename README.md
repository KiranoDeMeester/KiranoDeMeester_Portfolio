## Database

This project uses a MySQL database named `portfolio`.

To set up locally:
1. Create a database called `portfolio`
2. Import `database/schema.sql`
3. Configure credentials in the `.env` file
